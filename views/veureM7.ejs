<!DOCTYPE html>
<html>
<head>
    <title>m7</title>
    <!-- Agregar el enlace a la hoja de estilos de Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="style.css">
    <style>
        .button-container {
            position: fixed;
            top: 10px;
            left: 10px;
        }
        .button-container a {
            display: inline-block;
            transition: transform 0.2s;
        }
        .button-container a:hover {
            transform: scale(1.1);
        }
        .img-zoom img:hover {
            transform: scale(1.1);
            transition: transform 0.2s;
        }
        .img-zoom a {
            text-decoration: none;
            border: none;
            padding: 0;
            margin: 0;
            outline: none;
        }
        .documento {
            border: 1px solid #ddd;
            padding: 10px;
            margin: 10px;
            text-align: center;
        }
    </style>
</head>
<body style="background-color: rgb(228, 228, 228)">
    <div class="button-container">
        <a href="/m7">
            <img src="/Fons/Registre/felechaTornarEnrera.png" alt="TORNAR">
        </a>
    </div>
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Contingut</h1>
        <div class="row">
            <% arxius.forEach(documento => { %>
              <% if (documento.nom_modul === 'M7') { %>
                <div class="col-md-4">
                  <div class="documento">
                    <p>Nom Document: <%= documento.nom %></p>
                    <p>Descripció: <%= documento.descripcio %></p>
                    <% if (documento.arxiu) { %>
                      <a href="#" target="_blank" onclick="mostrarDocumento('<%= documento.arxiu.toString('base64') %>')">Veure Document</a>
                    <% } else { %>
                      <p>Documento no disponible</p>
                    <% } %>
                  </div>
                </div>
              <% } %>
            <% }); %>
          </div>
        <script>
            function mostrarDocumento(base64) {
              const pdfWindow = window.open('');
              pdfWindow.document.write('<html><head><title>Document</title></head><body style="margin:0;"><embed width="100%" height="100%" src="data:application/pdf;base64,' + base64 + '" type="application/pdf"></embed></body></html>');
            }
          </script>
    </div>

    <!-- Agregar la referencia a la librería de Bootstrap y jQuery (necesario para algunas funcionalidades) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>