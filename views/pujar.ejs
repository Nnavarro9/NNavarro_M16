<!DOCTYPE html>
<html>
<head>
    <title>Pujar arxius</title>
    <!-- Agregar el enlace a la hoja de estilos de Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="style.css">
    <style>
        .button-container {
            position: fixed;
            top: 10px;
            left: 10px;
        }
        .button-container a {
            display: inline-block;
            transition: transform 0.2s;
        }
        .button-container a:hover {
            transform: scale(1.1);
        }
        .img-zoom img:hover {
            transform: scale(1.1);
            transition: transform 0.2s;
        }
        .img-zoom a {
            text-decoration: none;
            border: none;
            padding: 0;
            margin: 0;
            outline: none;
        }
    </style>
</head>
<body style="background-color: rgb(228, 228, 228)">
    <div class="button-container">
        <a href="/apunts">
            <img src="/Fons/Registre/felechaTornarEnrera.png" alt="TORNAR">
        </a>
    </div>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-auto">
                <h1>Pujar Documents</h1>
                <form action="/upload" method="post" enctype="multipart/form-data">
                    <label for="documento">Selecciona un documento PDF:</label>
                    <input type="file" name="documento" id="documento" accept=".pdf" required>
                    <br>
                    <label for="nom_modul">Nom modul:</label>
                    <input type="text" name="nom_modul" id="modul" required>
                    <br>
                    <label for="nom_doc">Nom document:</label>
                    <input type="text" name="nom_doc" id="nom_doc" required>
                    <br>
                    <label for="descripcio">Descripció:</label>
                    <input type="text" name="descripcio" id="descripcio" required>
                    <br>
                    <button type="submit">Pujar Document</button>
                  </form>
                  <form action="/vuereM1" method="get">
                    <button type="submit">Veure Documents</button>
                  </form>
                
            </div>
        </div>
    </div>
    <script>
        // Leer el parámetro de la URL
        const urlParams = new URLSearchParams(window.location.search);
        const mensaje = urlParams.get('mensaje');
      
        // Mostrar el mensaje si está presente
        if (mensaje) {
          alert(mensaje);
        }
    </script>
    <!-- Agregar la referencia a la librería de Bootstrap y jQuery (necesario para algunas funcionalidades) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>